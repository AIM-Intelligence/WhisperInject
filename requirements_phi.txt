# Requirements for Phi-4 model
# Use separate venv: python -m venv venv_phi && source venv_phi/bin/activate

# Core PyTorch
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Transformers (PINNED for Phi-4 compatibility)
transformers==4.48.2
peft==0.13.0

# Other HuggingFace
accelerate>=0.20.0
datasets>=2.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
gtts>=2.5.0

# For model internals
timm>=1.0.17
backoff>=2.2.1

# Attacks
sentence-transformers>=2.2.0
scipy>=1.10.0
numpy>=1.24.0

# LLM evaluation
openai>=1.0.0

# Utils
python-dotenv>=1.0.0
tqdm>=4.65.0

